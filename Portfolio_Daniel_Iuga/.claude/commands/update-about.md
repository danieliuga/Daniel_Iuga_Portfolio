Actualiza la sección About del portfolio.

Opciones disponibles:
1. **Stats** (años de experiencia, proyectos, idiomas) — editar el array `stats` en `src/Components/About.tsx`.
2. **Bio principal** — editar el párrafo `<p>` con la descripción personal en `src/Components/About.tsx`.
3. **Título de rol** — editar el `<h3>` con "Junior Frontend Developer" en `src/Components/About.tsx`.
4. **Datos personales** (ciudad, fecha de nacimiento) — editar los spans al final del componente.

Pasos:
1. Pregunta al usuario qué quiere actualizar y cuál es el nuevo valor.
2. Edita `src/Components/About.tsx` con los cambios indicados.
3. Ejecuta `npm run build` para verificar que no hay errores.
